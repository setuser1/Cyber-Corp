import algo

send = str(input("Enter msg: "))

# recieve = str(input("Enter recieved msg: ")) # uncomment if needed

send = algo.encode(send)
print(send)

# recieve = algo.decode(recieve) # uncomment if needed
# print(recieve)
