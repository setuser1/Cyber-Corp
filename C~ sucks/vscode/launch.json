{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Run C~ Program",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/.vscode/wrapper.py",
            "args": [
                "${file}"
            ],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
